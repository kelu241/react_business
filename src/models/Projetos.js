export class Projetos {
  constructor(id, nome, descricao, dataInicio, dataFim, status) {
    this.id = id;
    this.nome = nome;
    this.descricao = descricao;
    this.dataInicio = dataInicio;
    this.dataFim = dataFim;
    this.status = status;
  }

}